spring:
  application:
    name: lista-pia
  jpa:
    open-in-view: false
    properties:
      hibernate:
        default_schema: public

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
