spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/onac_lista_pia}
    username: ${POSTGRES_USER:onac}
    password: ${POSTGRES_PASSWORD:onac123}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
